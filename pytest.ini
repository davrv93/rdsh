[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --disable-warnings
markers =
    integracion: pruebas que requieren el almacén levantado (docker compose up -d warehouse)
filterwarnings =
    ignore::DeprecationWarning
